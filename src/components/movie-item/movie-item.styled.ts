import styled from 'styled-components';

const MovieItem = styled.li`
  padding: 25px 20px;
`;

const MoviePoster = styled.img``;

const MovieTitle = styled.h3``;

export { MovieItem, MovieTitle, MoviePoster };
